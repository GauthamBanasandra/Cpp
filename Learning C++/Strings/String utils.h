/*
* In the header file, only the namespace where string is defined (std) is sufficient.
* Notice that the string header file isn't included.
*/
#pragma once
using namespace std;

int compare(string, string);
int lexicographic_comparison(string, string);